global:
  installs:
    cmd: |
      cargo install fnm
      fnm install lts-latest
      fnm env --use-on-cd | Out-String | Invoke-Expression
      corepack enable
    depends: [rust]
  links:
    .npmrc: ~/.npmrc
