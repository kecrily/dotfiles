global:
  links:
    .gitconfig: ~/.gitconfig

windows:
  installs: |
    winget install Git.Git -h
    git config --global gpg.program "C:\\Program Files (x86)\\GnuPG\\bin\\gpg.exe"
    winget install GitHub.Cli -h

darwin:
  installs: brew install gh
